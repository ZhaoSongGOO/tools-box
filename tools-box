#!/bin/bash

SCRIPT_PATH=$(dirname "$0")

TOOLS_BOX_SOURCE_RELATIVE_PATH="."

TOOLS_BOX_SOURCE_PATH="${SCRIPT_PATH}"/"${TOOLS_BOX_SOURCE_RELATIVE_PATH}"
OLD_PYTHONPATH=$PYTHONPATH
export PYTHONPATH=${TOOLS_BOX_SOURCE_PATH}:${OLD_PYTHONPATH}
python3 "${TOOLS_BOX_SOURCE_PATH}"/app.py "$@"
